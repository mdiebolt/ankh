main.h-screen.flex.flex-col.justify-between
  .max-w-3xl.mx-auto
    .p-12
      .text-sm.text-zinc-300 Add card to
      h1.text-5xl.font-black.text-pretty
        = @deck.name

    .mx-auto.bg-zinc-800.rounded-xl.px-6.py-4.text-pretty
      = form_with model: [@deck, @card], class: "h-full flex flex-col justify-between" do |form|
        div
          = form.label :front
          = form.rich_text_area :front

        .mt-4
          = form.label :back
          = form.rich_text_area :back

        .mt-8
          = form.submit "Create", class: "w-32 py-1 border border-zinc-600 rounded-full cursor-pointer hover:bg-zinc-600 active:bg-zinc-700"
